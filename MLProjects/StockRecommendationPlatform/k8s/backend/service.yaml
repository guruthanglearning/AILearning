apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: stockresearch
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8010
      targetPort: 8010
      nodePort: 30810
